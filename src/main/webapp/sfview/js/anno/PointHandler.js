import Handler from"/sfview/js/anno/Handler.js";import Ellipse from"/sfview/js/anno/Ellipse.js";export default class PointHandler extends Handler{x;y;calcStep;constructor(getPosition,calcStep){super("grab",getPosition);this.calcStep=calcStep}draw(ctx,shape){let[cx,cy]=this.getPosition(shape);Ellipse.draw(ctx,cx,cy)}contains(shape,rx,ry){let[cx,cy]=this.getPosition(shape);return Math.pow(rx-cx,2)+Math.pow(ry-cy,2)<Math.pow(Handler.WIDTH/2,2)}initStep(x,y){this.x=x;this.y=y}}