import Handler from"/sfview/js/anno/Handler.js";import Ellipse from"/sfview/js/anno/Ellipse.js";export default class RotateHandler extends Handler{stepAngle;constructor(getPosition){super("grab",getPosition,null)}draw(ctx,shape){let[cx,cy]=this.getPosition(shape);Ellipse.draw(ctx,cx,cy)}contains(shape,rx,ry){let[cx,cy]=this.getPosition(shape);return Math.pow(rx-cx,2)+Math.pow(ry-cy,2)<Math.pow(Handler.WIDTH/2,2)}calcStep(shape,x,y){this.stepAngle=shape.getStepAngle(x,y)}applyStep(shape){shape.setStepAngle(this.stepAngle)}}